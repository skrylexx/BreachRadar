# ─── Backend FastAPI — Dépendances Python ───────────────────────────────────
# FastAPI + Uvicorn
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Base de données
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Redis (sessions + rate limiting)
redis[hiredis]==5.2.1

# Authentification & Sécurité
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyotp==2.9.0                    # TOTP RFC 6238 (Google Auth, Authy...)
qrcode[pil]==8.0               # QR code pour setup MFA
python-multipart==0.0.18       # Form parsing

# Rate limiting
slowapi==0.1.9

# Validation
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Mail (notifications + reset password)
aiosmtplib==3.0.2
jinja2==3.1.5                  # Templates email

# Utilitaires
python-dotenv==1.0.1
httpx==0.27.2                  # Client HTTP async (proxy vers CLI BreachRadar)
